import React from 'react';

import { Container, Main, Board } from 'components';

const Home: React.FC = () => {
	return (
		<Container>
			<Board>
				<Main />
			</Board>
		</Container>
	);
};

export default Home;
